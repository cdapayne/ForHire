<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Successful - JobbyJobJob API</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        brand: { 400: '#4ade80', 500: '#00ff88', 600: '#16a34a' },
                        dark: { 800: '#0f172a', 900: '#0a1929' }
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-dark-900 text-gray-100 antialiased min-h-screen flex items-center justify-center">
    <div class="text-center max-w-md mx-auto px-4">
        <div class="w-20 h-20 bg-green-500/10 rounded-full flex items-center justify-center mx-auto mb-6">
            <i class="fas fa-check text-green-400 text-4xl"></i>
        </div>
        <h1 class="text-3xl font-bold mb-4">Payment Successful!</h1>
        <p class="text-gray-400 mb-6">
            Your subscription to the <span class="text-brand-500 font-semibold capitalize"><%= plan || 'Pro' %></span> plan is now active.
        </p>
        <div class="bg-gray-800/50 border border-gray-700 rounded-xl p-6 mb-6">
            <p class="text-sm text-gray-400">Your new limits are now in effect:</p>
            <ul class="mt-4 space-y-2 text-left">
                <% if (plan === 'starter') { %>
                    <li><i class="fas fa-check text-brand-500 mr-2"></i> 100,000 job rows per month</li>
                    <li><i class="fas fa-check text-brand-500 mr-2"></i> Up to 50 location filters</li>
                    <li><i class="fas fa-check text-brand-500 mr-2"></i> 60 requests per minute</li>
                <% } else if (plan === 'pro') { %>
                    <li><i class="fas fa-check text-brand-500 mr-2"></i> 500,000 job rows per month</li>
                    <li><i class="fas fa-check text-brand-500 mr-2"></i> Unlimited location filters</li>
                    <li><i class="fas fa-check text-brand-500 mr-2"></i> 200 requests per minute</li>
                <% } else if (plan === 'enterprise') { %>
                    <li><i class="fas fa-check text-brand-500 mr-2"></i> 1,000,000 job rows per month</li>
                    <li><i class="fas fa-check text-brand-500 mr-2"></i> Unlimited location filters</li>
                    <li><i class="fas fa-check text-brand-500 mr-2"></i> 1,000 requests per minute</li>
                <% } %>
            </ul>
        </div>
        <a href="/dashboard" class="inline-flex items-center bg-brand-500 text-dark-900 px-6 py-3 rounded-lg font-semibold hover:bg-brand-400 transition">
            <i class="fas fa-arrow-right mr-2"></i> Go to Dashboard
        </a>
    </div>
</body>
</html>
